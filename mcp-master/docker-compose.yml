version: '3.9'

services:
  mcp-master:
    build: .
    image: mcp-master:latest
    container_name: mcp-master

    env_file:
      - .env
 
    environment:
      - NODE_ENV=production
      - MCP_LOG_LEVEL=info
      - PORT=3080
    ports:
      - "3080:3080"
    restart: unless-stopped
